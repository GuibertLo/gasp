/**
 * @typedef CategoryData
 * @prop { number } id - The Category's unique identifier
 * @prop { string } name - The Category's name
 */
interface CategoryData {
  id: number;
  name: string;
}

export type { CategoryData };
